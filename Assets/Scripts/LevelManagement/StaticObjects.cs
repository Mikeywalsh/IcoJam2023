using UnityEngine;

public class StaticObjects : MonoBehaviour
{
    private StaticObjects _instance;

    private void Awake()
    {
        if (_instance == null)
        {
            _instance = this;
        }
        else
        {
            Destroy(gameObject);
            return;
        }

        DontDestroyOnLoad(gameObject);
    }
}